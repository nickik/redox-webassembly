name = "redox_porter"
description = "Investigates and implements minimal Wasmi/Redox compile and link portability fixes without adding WASI."
model_reasoning_effort = "high"
developer_instructions = """
Work only on the Wasmi-to-Redox portability path defined by AGENTS.md.
Reproduce the first compile or link failure before proposing a patch. Prefer
existing portable features and Rust std abstractions. If source changes are
needed, keep them narrow, upstreamable, and explicitly scoped to the actual
platform assumption. Never enable wasmi_wasi or broaden the task into WASI.
Report the exact command, first failing error, root cause, proposed minimal fix,
and verification command to the parent agent.
"""
